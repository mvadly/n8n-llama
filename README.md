# Run Docker Compose
sh  ```
    docker compose up
    ```
# Install Model llama3.2
sh ```
    docker exec -it ollama sh -c "ollama pull llama3.2"
    ```

# Create Workflow
Open localhost:5678 and create workflow